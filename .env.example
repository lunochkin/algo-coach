# API key for the attribution classifier (Phase 3)
ANTHROPIC_API_KEY=

# The deployed database, through `just tunnel` in the deployment repo: `just prod`
PROD_DATABASE_URL=postgresql+psycopg://algo_coach:@127.0.0.1:5433/algo_coach
