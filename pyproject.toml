[project]
name = "algo-coach"
version = "0.1.0"
description = "AI coaching layer for deliberate practice of algorithmic problem-solving: execution-verified SRS over a technique-mastery model with LLM failure diagnosis"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.14"
authors = [{ name = "Maksim Lunochkin" }]
dependencies = [
    "pydantic>=2.7",
]

[project.scripts]
algo-coach = "algo_coach.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.16.0",
]

[tool.pytest.ini_options]
python_classes = []

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "SIM"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/algo_coach"]
